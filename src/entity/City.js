export default class City {
  constructor(id,cityName,createdTime,addressList) {
    this.id = id
    this.cityName = cityName
    this.createdTime = createdTime
    this.addressList = addressList
  }
}